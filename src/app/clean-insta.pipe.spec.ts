import { CleanInstaPipe } from './clean-insta.pipe';

describe('CleanInstaPipe', () => {
  it('create an instance', () => {
    const pipe = new CleanInstaPipe();
    expect(pipe).toBeTruthy();
  });
});
